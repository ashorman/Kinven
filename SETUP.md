# Kinven setup

You need [Node.js](https://nodejs.org/) 20 or newer.

```bash
git clone https://github.com/ashorman/Kinven.git
cd Kinven
npm install
npm run dev
```

Then open the local URL Vite prints (usually `http://127.0.0.1:5173`) in a browser on the same computer.
